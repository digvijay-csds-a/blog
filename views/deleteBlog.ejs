<!-- adminUsers.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - User List</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <style>
        .text-center {
            text-decoration: underline;
            font-family: 'Courier New', Courier, monospace;
            font-size: 40px;
            color: white;
        }
        .delete-icon {
            color: red;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-light bg-light">
        <a class="navbar-brand" href="#">
            <img src="./logo2.png" width="120" height="70" alt="Logo">
        </a>
        <span class="fs-1" style="font-family: 'Courier New', Courier, monospace">
            Admin
        </span>
    </nav>
    <div class="container-fluid mt-1">
        <h2 class="text-center bg-success">Delete Blog</h2>
        <table class="table table-striped border border-dark border-2">
            <thead>
                <tr class="fs-3" style="font-family: 'Times New Roman', Times, serif;">
                    <th class="col-11">Title</th>
                    <th class="col-1">Actions</th>
                </tr>
            </thead>
            <tbody>
                <% blogs.forEach(blog=> { %>
                    <tr>
                        <td><%= blog.title %></td>
                        <td>
                            <form action="/deleteB" method="POST" style="display: inline;" class="ms-auto">
                                <input type="hidden" name="blog_id" value="<%= blog.id %>">
                                <button class="btn btn-danger" 
                                onclick="return confirm('Are you sure you want to delete this role?');"
                                style="margin-left:10px; border-radius:20px">Delete</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
    <script>
 

    </script>
</body>
</html>
