<!-- adminUsers.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - User List</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <style>
        .text-center {
            text-decoration: underline;
            font-family: 'Courier New', Courier, monospace;
            font-size: 40px;
            color: white;
        }
        .delete-icon {
            color: red;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-light bg-light">
        <a class="navbar-brand" href="#">
            <img src="./logo2.png" width="120" height="70" alt="Logo">
        </a>
        <span class="fs-1" style="font-family: 'Courier New', Courier, monospace">
            Admin
        </span>
    </nav>
    <div class="container-fluid mt-1">
        <h2 class="text-center bg-success">Delete Role</h2>
        <table class="table table-striped border border-dark border-2">
            <thead>
                <tr class="fs-3" style="font-family: 'Times New Roman', Times, serif;">
                    <th class="col-11">Role</th>
                    <th class="col-1">Actions</th>
                </tr>
            </thead>
            <tbody>
                <% roles.forEach(role => { %>
                    <tr>
                        <td><%= role.role %></td>
                        <td>
                            <form action="/deleteR" method="POST" style="display: inline;" class="ms-auto">
                                <input type="hidden" name="role_id" value="<%= role.id %>">
                                <button class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this role?');" style="margin-left:10px; border-radius:20px">Delete</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
    <script>
        function confirmDelete(userId) {
            const confirmDelete = confirm("Are you sure you want to delete this role?");
            if (confirmDelete) {
                // Redirect to delete route with user ID
                window.location.href = `/deleteR`;
            }
        }
    </script>
</body>
</html>
